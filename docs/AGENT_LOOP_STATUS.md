The bounded self-repair engine is implemented in `benchmark/agent_loop.py` and covered by deterministic tests.

The dashboard integration is intentionally gated behind a model adapter and will be enabled after the adapter is wired to the current model-service API.
