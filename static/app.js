const state={context:null,outputs:{qwen:'',nemotron:''},executions:{}};const $=x=>document.getElementById(x);const note=(t,e=false)=>{$('notice').textContent=t;$('notice').classList.toggle('error',e)};const task=()=>$('task').value.trim();const body=()=>({repo_url:$('repo').value.trim(),task:task(),context:state.context,outputs:state.outputs,executions:state.executions,reference_code:$('reference').value.trim()||null,max_attempts:+$('budget').value});async function post(url,data){const r=await fetch(url,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)}),d=await r.json();if(!r.ok)throw Error(d.detail||'Request failed');return d}function busy(v,t){document.querySelectorAll('button').forEach(b=>b.disabled=v);if(t)note(t)}
async function load(){try{const ts=await (await fetch('/api/tasks')).json();$('taskSelect').innerHTML='<option value="">Custom task</option>'+ts.map(x=>`<option value="${x.task.replaceAll('"','&quot;')}">${x.id} · ${x.category}</option>`).join('')}catch(e){note(e.message,true)}}load();$('taskSelect').onchange=e=>{if(e.target.value)$('task').value=e.target.value};$('budget').oninput=e=>$('budgetValue').textContent=e.target.value;
$('ingest').onclick=async()=>{if(!$('repo').value.trim())return note('Enter a public GitHub URL.',true);busy(true,'Ingesting repository and redacting obvious secrets…');try{state.context=await post('/api/ingest',{repo_url:$('repo').value});$('connection').textContent='● Repository ready';$('connection').style.color='var(--lime)';$('repoInfo').textContent=`Loaded · ${state.context.redactions||0} redaction(s)`;note('Repository ready. Choose a task and generate.')}catch(e){note(e.message,true)}finally{busy(false)}};
$('generate').onclick=async()=>{if(!state.context)return note('Ingest a repository first.',true);if(!task())return note('Describe or select a task first.',true);state.outputs={qwen:'',nemotron:''};$('qwen').textContent=$('nemotron').textContent='Generating…';busy(true,'Both models are generating against identical context…');try{const r=await fetch('/api/generate',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({task:task(),context:state.context})});if(!r.ok)throw Error((await r.json()).detail);const reader=r.body.getReader(),decoder=new TextDecoder();let buf='';for(;;){const{value,done}=await reader.read();if(done)break;buf+=decoder.decode(value,{stream:true});const chunks=buf.split('\n\n');buf=chunks.pop();for(const c of chunks){const ev=(c.match(/^event: (.+)$/m)||[])[1],raw=(c.match(/^data: (.+)$/m)||[])[1];if(!raw)continue;const d=JSON.parse(raw);if(ev==='chunk'){state.outputs[d.model]+=d.text;$(d.model).textContent=state.outputs[d.model]}if(ev==='error')throw Error(d.message)}}note('Candidates ready. Choose an evidence action.')}catch(e){note(e.message,true)}finally{busy(false)}};
function show(data,title){$('results').hidden=false;$('results').querySelector('h2').textContent=title;if(data.retrieval){$('summary').innerHTML=`<div class="metric"><small>SELECTED FILES</small><strong>${data.retrieval.selected_files}</strong></div><div class="metric"><small>CANDIDATES</small><strong>${data.retrieval.candidate_files.length}</strong></div><div class="metric"><small>CONTEXT CHARS</small><strong>${data.retrieval.total_chars}</strong></div>`;$('details').innerHTML=`<div class="detail">${data.retrieval.files.join('<br>')}</div>`;return}$('summary').innerHTML=Object.entries(data.results||{}).map(([k,v])=>`<div class="metric"><small>${k.toUpperCase()} OVERALL</small><strong>${v.overall_score_10??'—'}/10</strong><small>${v.confidence_note||v.error||''}</small></div>`).join('')+`<div class="metric"><small>PAIRWISE WINNER</small><strong>${data.pairwise?.winner||'—'}</strong><small>${data.pairwise?.reason||''}</small></div>`;$('details').innerHTML=Object.entries(data.results||{}).map(([k,v])=>`<div class="detail"><b>${k.toUpperCase()} DETAIL</b><pre>${JSON.stringify(v.detailed_metrics||v.validation||v,null,2)}</pre></div>`).join('')}
document.querySelectorAll('[data-action]').forEach(b=>b.onclick=async()=>{if(!state.context||!state.outputs.qwen||!state.outputs.nemotron)return note('Ingest then generate both candidates first.',true);const a=b.dataset.action;busy(true,a==='evaluate'?'Judging candidates…':a==='sandbox'?'Running restricted checks…':a==='repair'?'Running bounded self-repair…':'Inspecting retrieval…');try{if(a==='retrieval')show({retrieval:await post('/api/retrieval',{task:task(),context:state.context})},'Retrieval evidence');else{const d=await post('/api/'+a,body());if(a==='sandbox'){state.executions=d.executions;show({results:Object.fromEntries(Object.entries(d.executions).map(([k,v])=>[k,{validation:v}]))},'Isolated execution');note('Checks complete. Validate again to include execution evidence.')}else if(a==='repair'){state.outputs=d.outputs;state.executions=d.executions;$('qwen').textContent=d.outputs.qwen;$('nemotron').textContent=d.outputs.nemotron;note('Repair complete. Validate again for semantic scores.')}else show(d,'Decision report')}}catch(e){note(e.message,true)}finally{busy(false)}});document.querySelectorAll('[data-copy]').forEach(b=>b.onclick=()=>navigator.clipboard.writeText(state.outputs[b.dataset.copy]||''));
